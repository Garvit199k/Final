{
  "name": "typing-test-game",
  "version": "1.0.0",
  "description": "A typing test and dog rescue game platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd public && npx live-server --port=8080 --no-browser",
    "dev:all": "concurrently \"npm run dev\" \"npm run client\"",
    "build": "echo 'No build step required'",
    "vercel-build": "echo 'Build step completed'"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "nodemon": "^3.0.1"
  }
}
